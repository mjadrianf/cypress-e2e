pipeline {
  agent {
    // this image provides everything needed to run Cypress
    docker {
      image 'cypress/base:13'
    }
  }

  stages {
      steps {
        sh 'npm ci'
        sh "npm run cy:run"
      }
    }
  }
}
